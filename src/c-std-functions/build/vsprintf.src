	section	.text
	assume	adl = 1
	section	.text
	public	_vsprintf
_vsprintf:
	call	__frameset0
	ld	hl, (ix + 6)
	ld	de, (ix + 9)
	ld	bc, (ix + 12)
	ld	(ix + 6), hl
	ld	(ix + 9), de
	ld	(ix + 12), bc
	pop	ix
	jq	__u_print
	section	.text

	extern	_llvm.stackprotector
	extern	__frameset0
	extern	__u_print
